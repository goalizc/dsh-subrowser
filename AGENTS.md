# AGENTS.md — dsh-subrowser 开发约定

本文件为 AI 助手在本仓库内工作时必须遵守的约定。宿主侧（DSH）不对本文件做任何解析，仅为开发协作约定。

## 版本管理（正式版，必须遵守）

插件自 v1.0.0 起进入正式版。**本次及以后的每一次功能改动、Bug 修复、行为调整，都必须同步递增版本号。**

版本号定义（语义化）：

- **Patch**（`1.0.0 → 1.0.1`）：Bug 修复、样式微调、非行为性改动
- **Minor**（`1.0.1 → 1.1.0`）：新增功能、行为改进，且向后兼容
- **Major**（`1.1.0 → 2.0.0`）：不兼容变更、破坏性改动

提交前必须将版本号在三处保持完全一致：

1. `package.json` 的 `version` 字段
2. `assets/subrowser.js` 中 `SB.v` 常量
3. 发布说明（如 README / CHANGELOG 若存在）

判断是否需要递增：任何会改变用户可感知行为的改动（含纯前端、纯样式）都必须递增；仅注释、仅格式、仅文档化的改动不强制递增。

## 架构硬约束（源自项目经验）

- 纯前端实现，窗口所有交互在 `assets/subrowser.js` 单文件内；`lib/index.js` 只提供静态路由 `/dsh-subrowser/widget.js`，按 mtime 热读前端脚本。
- 窗口尺寸下限 `240×180`，必须约束在视口内；最多 8 个窗口。
- 最小化用 `visibility:hidden`（保留 iframe 滚动位置），不可用 `display:none`。
- 地址栏折叠用 `visibility:hidden` 保留占位；折叠后地址栏区域仍可拖拽窗口（交互区排除中不可含整个 `.sbr-addr-wrap`）。
- iframe 必须显式 `width/height:100%`，否则 UA 默认 300px 造成内容窄渲染。
- 滚动条隐藏通过宽/高拉伸裁剪右/下边缘实现（左上角不动），步进 1px。
- Esc 仅对 DSH 主界面生效；跨域 iframe 内按键受浏览器同源隔离，父页面无法捕获（不做）。